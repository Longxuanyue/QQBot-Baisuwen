# 白苏文（BaiSuWen）QQ Bot 项目安全评估报告

| 项目 | 内容 |
|---|---|
| 评估对象 | `d:\Nonebot\baisuwen`（NoneBot2 + OneBot V11 智能对话 QQ 机器人） |
| 评估日期 | 2026-08-01 |
| 评估方式 | 静态代码审查（人工逐模块审阅 + 危险函数模式扫描） |
| 覆盖范围 | `src/plugins/` 全部 13 个插件、`deploy/`、`tools/`、配置与依赖清单、Git 历史、运行环境 |
| 风险统计 | 高危 5 项 / 中危 8 项 / 低危 9 项 |

---

## 一、项目概览

本项目为基于 NoneBot2 + DeepSeek LLM 的 QQ 智能伴侣机器人，主要模块：

- **核心对话插件**（`nonebot_plugin_update_baisuwen`）：LLM 对话编排、人设、记忆生成、语音/图片处理
- **Web 管理后台**（`nonebot_plugin_webui`）：FastAPI 子应用，含登录认证、配置编辑、插件开关、记忆浏览、备份恢复、审计日志
- **管理命令**（`nonebot_plugin_admin`）：仅超级用户可用的状态/记忆/休眠命令
- **记忆系统**（`nonebot_plugin_memory`）：SQLite 多用户记忆库（短/长期）
- **ASR / TTS**（`nonebot_plugin_asr`、`nonebot_plugin_tts`）：Whisper 语音识别、VITS / GPT-SoVITS 语音合成
- **多模态**（`nonebot_plugin_multimodal`）：图片下载与 LLM Vision 理解
- **游戏新闻**（`nonebot_plugin_gamenews`）：活动数据渲染推送（Playwright 截图）
- 其余：对话管理、情感分析、用户画像、卫戍协议查询、帮助索引，及外部商店插件（skland/status/docs 等）

---

## 二、基础逻辑性代码安全问题

### 【高危】

#### H1. WebUI 前端存在多处未转义 HTML 渲染（存储型 XSS 风险面）

- **位置**：[audit_log.html:64-73](src/plugins/nonebot_plugin_webui/templates/audit_log.html#L64-L73)、[memory_viewer.html:107-120](src/plugins/nonebot_plugin_webui/templates/memory_viewer.html#L107-L120)、[plugins.html](src/plugins/nonebot_plugin_webui/templates/plugins.html)、[backup.html:99](src/plugins/nonebot_plugin_webui/templates/backup.html#L99)、[config_env.html:68](src/plugins/nonebot_plugin_webui/templates/config_env.html#L68)
- **问题**：审计日志页将 `e.ts / e.user / e.action / e.target / e.detail / e.ip` 直接以模板字符串拼入 `innerHTML`，**均未做 HTML 转义**。其中 `e.detail`、`e.target` 可携带用户可控内容（如记忆删除操作中拼接的 `memory_id`、路径参数 `user_id`），`e.ip` 来自可伪造的 `X-Forwarded-For` 请求头（见 H4）。当前可写入审计的入口均为 super 权限操作，**直接可利用性中等**；但一旦任何审计条目中落入攻击者可控字符串（或未来在日志中加入消息内容），即形成存储型 XSS。
- **影响**：结合 H2（Cookie 无 HttpOnly），XSS 可直接窃取管理员会话，获得 WebUI 全部权限（读 .env 密钥、改配置、备份下载、重启 Bot）。

#### H2. Session Cookie 缺少 HttpOnly / Secure 属性（XSS 窃取会话）

- **位置**：[login.html:112、138](src/plugins/nonebot_plugin_webui/templates/login.html#L112-L138)
- **问题**：会话 Cookie `webui_session` 由前端 JS 通过 `document.cookie` 写入，**未设置 `HttpOnly`**（`SameSite=Strict` 已设置，缓解了 CSRF）。任何 XSS（见 H1）或浏览器扩展、同机恶意页面均可读取该 Cookie 直接冒充管理员。
- **影响**：管理员会话可被窃取 → 完全接管 WebUI。
- **建议**：改为由服务端 `Set-Cookie` 下发（`HttpOnly; SameSite=Strict; Secure(HTTPS 时)`），前端 JS 不再触碰 Cookie。

#### H3. 图片下载文件名未消毒 —— 路径穿越写入风险

- **位置**：[image_handler.py:18-34](src/plugins/nonebot_plugin_multimodal/image_handler.py#L18-L34)（`download_image` 中 `os.path.join(IMAGE_CACHE_DIR, file_name)`，`file_name` 直接来自消息段的 `file`/`file_id` 字段）
- **问题**：OneBot 消息中图片段的 `file` 字段由协议端（NapCat 等）填充，部分实现允许发送自定义路径/任意字符串。若 `file` 形如 `..\..\start_bot.bat`，下载内容将被写入目录之外；配合可控的图片 URL，可形成**任意文件写入**。
- **影响**：可覆盖 `.env`（植入恶意密钥）、插件源码（`.py`）或启动脚本 → 进程重启后代码执行（RCE）。
- **建议**：对文件名取 `basename` 并白名单后缀，禁止 `..`、`\`、`/`、绝对路径。

#### H4. 语音/图片 URL 任意下载：SSRF + 磁盘耗尽 + 无鉴权外联

- **位置**：[utils.py:19-33](src/plugins/nonebot_plugin_update_baisuwen/utils.py#L19-L33)（`download_voice_file` 直接下载 `file_id` 中的任意 `http(s)://` 地址）、[utils.py:164-186](src/plugins/nonebot_plugin_update_baisuwen/utils.py#L164-L186)、[image_handler.py:22-31](src/plugins/nonebot_plugin_multimodal/image_handler.py#L22-L31)
- **问题**：
  1. 消息段 `url` 由消息内容携带，未校验目标主机，Bot 会代用户请求任意内网/公网地址（**SSRF**），可探测内网服务；
  2. 下载**无大小限制、无连接超时兜底**（aiohttp 默认无超时），恶意大文件/慢连接可导致磁盘耗尽与事件循环阻塞；
  3. 下载内容直接交给 Whisper / 图片解析器处理，畸形文件可造成 CPU 高占用（解压炸弹类）。
- **影响**：内网探测、磁盘写满导致服务不可用、Bot 资源被消耗（账单一类风险在私聊 bot 场景不适用，但 DoS 适用）。
- **建议**：限制协议为 `https`、拒绝私有/链路本地地址段（RFC 1918 等）；限制响应大小（如 20MB）；设置下载超时；文件名取哈希且不带路径。

#### H5. 备份恢复 ZIP 解压未校验成员路径（Zip Slip）

- **位置**：[backup.py:94-95](src/plugins/nonebot_plugin_webui/backup.py#L94-L95)（`zf.extractall(tmp_dir)`）
- **问题**：`zipfile.extractall` 不阻止成员名中的 `../`，恶意 ZIP 可将文件写出临时目录，写入**进程权限可写的任意位置**（含项目根、系统临时目录、GPT-SoVITS 目录等）。
- **影响**：需要 `backup.restore` 权限（仅 super），但属典型的任意文件写入缺陷；与 H2/H1 叠加（若会话被窃取）即达 RCE。
- **建议**：逐成员校验 `Path(name).resolve()` 必须位于临时目录内；同时校验成员名白名单（仅 `.env`、`personality_traits.json`、`webui_plugin_states.json`、`user_data/*.db`）。

### 【中危】

#### M1. 记忆库路径由用户 ID 拼接，未消毒（路径穿越面）

- **位置**：[user_manager.py:16-21](src/plugins/nonebot_plugin_memory/user_manager.py#L16-L21)（`os.path.join(USER_DATA_DIR, f"short_{user_id}.db")`）
- **问题**：`user_id` 直接嵌入文件路径。WebUI 端 `/webui/api/memory/{user_id}` 的路径参数经 FastAPI 无法携带 `/`，但在 Windows 下反斜杠 `\` 同样作为路径分隔符，构造 `\..\..\x` 可越出 `user_data/` 目录创建/打开 `.db` 文件（需 `memory.view` 权限，admin 及以上）。
- **影响**：任意位置 SQLite 文件读写（若目标恰为含 `memories` 表的库可读取出数据）、目录外文件创建。
- **建议**：对 `user_id` 做白名单校验（仅数字），或强制 `basename` + 文件名正则。

#### M2. 密钥可经 WebUI 读取/备份导出（最小权限缺失）

- **位置**：[server.py:414-429](src/plugins/nonebot_plugin_webui/server.py#L414-L429)（`GET /api/config` 返回完整 `.env`，含 `DEEPSEEK_API_KEY`）、[backup.py:37-39](src/plugins/nonebot_plugin_webui/backup.py#L37-L39)（备份包含 `.env`）、权限矩阵 [auth.py:142-165](src/plugins/nonebot_plugin_webui/auth.py#L142-L165)（`config.view`、`backup.download` 对 **admin 角色开放**）
- **问题**：WebUI 的 admin（非 super）角色即可查看完整 `.env`（含 API Key）并可下载含密钥的备份包；密钥也随 `data/env_backups/` 以明文多次留存。
- **影响**：较低层级账号即可取得第三方 LLM 密钥；密钥泄露后产生费用消耗与数据滥用风险。
- **建议**：`config.view` 仅对 super 开放；备份包中密钥脱敏或对备份文件加密；`.env` 备份目录限制访问权限。

#### M3. 登录与 API 无速率限制

- **位置**：[server.py:339-360](src/plugins/nonebot_plugin_webui/server.py#L339-L360)（`/api/auth/status` 轮询）、[server.py:120-127](src/plugins/nonebot_plugin_webui/server.py#L120-L127)（`/login` 每次生成 token）
- **问题**：Token 为 128 bit 随机数（爆破难度大），但 `/login` 可被任意访问者无限调用（Token 内存存储无限增长，仅创建时清理），`/api/auth/status` 无频率限制。
- **影响**：内存 DoS；多账号并发验证场景下的暴力轮询。
- **建议**：对 `/login`、`/api/auth/status` 加 IP 级限流；Token 上限（如 100 个）并定期清理。

#### M4. 信任 `X-Forwarded-For` 请求头（审计 IP 污染 + XSS 注入源）

- **位置**：[server.py:105-110](src/plugins/nonebot_plugin_webui/server.py#L105-L110)
- **问题**：直接取 `X-Forwarded-For` 首值作为客户端 IP 写入审计日志，任意客户端可伪造；该字段随后被未转义渲染（见 H1），是存储型 XSS 的潜在注入通道。
- **建议**：仅在受信反向代理存在时使用 `X-Forwarded-For`，否则取 `request.client.host`；渲染前转义。

#### M5. 会话无登出、无吊销机制

- **位置**：[auth.py:85-115](src/plugins/nonebot_plugin_webui/auth.py#L85-L115)
- **问题**：Session 有效期 24 小时且密钥每次重启更换，但无 logout 端点，泄露的会话无法主动失效；TokenStore 中已验证 token 的 `user_id` 绑定无二次校验（换取会话后 token 即废弃，无问题），但 WS 通道与 HTTP 轮询并存增加了状态面。
- **建议**：提供登出接口（服务端维护会话黑名单或短生命周期 + 刷新机制）。

#### M6. FTS5 查询语法注入（低危害的参数化绕过面）

- **位置**：[retrieval.py:103-121](src/plugins/nonebot_plugin_memory/retrieval.py#L103-L121)
- **问题**：用户查询经 jieba 分词后直接以 `OR` 拼接为 FTS5 MATCH 查询串（以 `?` 参数绑定，无 SQL 注入），但引号/星号/`NEAR()` 等 FTS5 语法字符可能触发语法错误（已降级 LIKE）或改变匹配语义。
- **影响**：仅影响检索行为，无数据破坏；属健壮性/纵深问题。
- **建议**：对 token 过滤掉 FTS5 保留字符（`"`, `*`, `(`, `)`, `:` 等）。

#### M7. 模型文件反序列化未启用安全模式（供应链风险）

- **位置**：[vits/utils.py:148-150](src/plugins/nonebot_plugin_tts/vits/utils.py#L148-L150)（`torch.load(checkpoint_path, map_location='cpu')`）
- **问题**：`torch.load` 未指定 `weights_only=True`，PyTorch 默认 pickle 反序列化可执行任意代码。模型文件经 GitHub Releases / HuggingFace 分发（README 指引），若分发渠道被攻陷或模型文件被替换，加载即 RCE。
- **影响**：供应链投毒场景下的代码执行。
- **建议**：升级到支持 `weights_only=True` 的 PyTorch 版本并启用；校验模型文件哈希（如 SHA-256 与官方发布值比对）。

#### M8. 依赖版本未锁定（供应链完整性）

- **位置**：[requirements.txt](requirements.txt)（全部 `>=` 无上限）、[pyproject.toml](pyproject.toml)
- **问题**：`nonebot2>=2.5.0`、`httpx>=0.24.0`、`torch>=2.0.0` 等均未锁定版本；`deploy/deploy.py` 安装时无哈希校验。依赖上游发布恶意版本或引入回归时无法追溯。
- **建议**：为生产环境生成 lock 文件（`pip freeze` / `uv lock` / `pip-tools`），部署脚本优先使用锁定清单。

### 【低危】

| 编号 | 位置 | 问题 | 说明 |
|---|---|---|---|
| L1 | [renderer.py:24-33](src/plugins/nonebot_plugin_gamenews/renderer.py#L24-L33) | Jinja2 环境未开启 `autoescape` | 渲染数据来自本地抓取脚本（游戏站点标题），若标题含 HTML/JS 会在 Playwright 中执行；当前数据源受控，风险低 |
| L2 | [event_handler.py:292-296](src/plugins/nonebot_plugin_update_baisuwen/event_handler.py#L292-L296) | 日志记录用户消息全文与 LLM 回复 | 隐私泄漏面（日志/审计文件明文留存聊天内容） |
| L3 | [personality.py:58](src/plugins/nonebot_plugin_update_baisuwen/personality.py#L58)、[deploy.py:254](deploy/deploy.py#L254)、.env | QQ 号、群号硬编码/明文 | 信息暴露；群推送目标若被篡改可向任意群发消息（需改 .env，super 级） |
| L4 | `image_cache/`（249MB、302 个文件）、`voice_cache/` | 缓存无上限、无清理任务 | 长期运行磁盘耗尽；文件名基于 `hash()`（进程内随机）无法跨重启去重 |
| L5 | [websocket.py:26-52](src/plugins/nonebot_plugin_webui/websocket.py#L26-L52) | `/webui/ws` 端点无鉴权 | 任意本机进程可连接（watch_token 需已知 token 才有效）；广播订阅为预留功能 |
| L6 | [__init__.py:96-101](src/plugins/nonebot_plugin_webui/__init__.py#L96-L101) | 静态资源 `/webui/static` 公开挂载 | 仅 CSS/JS，信息暴露有限 |
| L7 | 记忆/人设数据库（`user_data/*.db`） | 本地明文存储全部用户隐私（对话记忆、画像） | 本机任意进程可读；建议磁盘加密/权限收紧 |
| L8 | [backup.py:56-64](src/plugins/nonebot_plugin_webui/backup.py#L56-L64) | 备份包明文打包（含记忆内容） | 下载备份即导出全部用户记忆与密钥，需日志留痕与访问控制 |
| L9 | [inference.py:48-54](src/plugins/nonebot_plugin_tts/inference.py#L48-L54) | TTS 合成无输入长度上限 | 超长文本进入合成管线（有分句处理，风险有限） |

---

## 三、运行环境安全问题

| 编号 | 项目 | 现状 | 风险说明 |
|---|---|---|---|
| E1 | **监听地址** | `.env` 中 `HOST=127.0.0.1`，端口 42200 | ✅ 正确。若改为 `0.0.0.0`，WebUI 与 OneBot WS 端口将对局域网/公网暴露，WebUI 登录流与 CSRF/XSS 攻击面全部变为远程可利用 —— **切勿修改**，如必须外网访问应置于反向代理 + HTTPS 之后 |
| E2 | **传输加密** | WebUI 走 HTTP 明文 | 仅本机访问时可接受；任何跨网络访问必须 TLS（否则会话 Cookie 与 .env 内容可被嗅探） |
| E3 | **API 密钥存储** | `DEEPSEEK_API_KEY` 明文存于 `.env`（已正确 gitignore，Git 历史无泄露） | ✅ 常规做法；风险在于：WebUI 可读（M2）、`data/env_backups/` 明文留存多个历史副本。建议近期轮换一次密钥并对备份目录加密 |
| E4 | **依赖环境** | venv：Python 3.12.9；nonebot2 2.5.0、fastapi 0.135.3、httpx 0.28.1、aiohttp 3.13.5、pydantic 2.12.5、starlette 1.0.0、playwright 1.58.0 | 版本较新，未发现已知高危 CVE 直接命中；但全部为 `>=` 安装，无锁文件（M8）。aiohttp/httpx 均需关注上游安全公告持续更新 |
| E5 | **磁盘增长** | `image_cache` 249MB / 302 文件，无清理机制 | 长时间运行（语音+图片+临时 wav）可写满磁盘导致 Bot 崩溃 |
| E6 | **外部代码加载** | [gpt_sovits_engine.py:39-46](src/plugins/nonebot_plugin_tts/gpt_sovits_engine.py#L39-L46) 将 `D:/GPT-SoVITS-main` 加入 `sys.path` 并 `os.chdir` 后导入其代码 | 该目录下第三方代码以 Bot 相同权限执行；需确保该目录无被外部写入的权限，且仅从官方渠道克隆 |
| E7 | **模型下载** | Whisper 首次运行自动从 HuggingFace 下载；模型文件经 GitHub Releases 分发 | 无完整性校验（见 M7），被劫持/替换时加载即执行 |
| E8 | **进程权限** | 以当前 Windows 用户权限运行，无沙箱 | Bot 具备完整用户权限（可写项目目录、读取用户数据）；一旦代码执行漏洞被利用，攻击者即获得该用户权限。建议使用受限账户运行 |
| E9 | **OneBot 协议端** | 反向 WS 仅连本机 127.0.0.1:42200 | ✅ 正确。若协议端（NapCat 等）开放远程端口，QQ 协议风险转移至协议端本身，需单独评估 |
| E10 | **文档服务** | `nonebot-plugin-docs` 挂载在线文档 | 公开只读，信息暴露有限 |

---

## 四、其他安全问题

### 1. LLM 提示词注入与记忆投毒（AI 层风险）

- **位置**：[event_handler.py:381-421](src/plugins/nonebot_plugin_update_baisuwen/event_handler.py#L381-L421)、[personality.py:40-110](src/plugins/nonebot_plugin_update_baisuwen/personality.py#L40-L110)、[event_handler.py:118-170](src/plugins/nonebot_plugin_update_baisuwen/event_handler.py#L118-L170)
- **风险**：用户消息 → 记忆库（LLM 抽取）→ 注入后续 system prompt，形成**记忆投毒**：恶意用户可在对话中引导 Bot 将"指令"写入长期记忆，之后持续操纵 Bot 输出（诱导泄露主人信息、传播恶意内容、越权操作）。群聊中任一成员均可发起。
- **缓解**：记忆检索结果与人设之间加"记忆仅供参考、不可覆盖系统指令"的边界说明；对记忆内容做敏感信息过滤；记忆写入前做指令性内容检测。

### 2. 隐私与数据留存

- 记忆库、人设、审计日志、聊天日志明文留存所有用户对话（L2/L7/L8）；私聊内容包含高度隐私（家庭成员、生活习惯、账号信息等）。
- 建议：明确留存周期与删除策略；备份加密；日志脱敏（QQ 号、消息内容）。

### 3. 多用户数据隔离语义

- 记忆按 QQ 号分库存储，Bot 回复时仅检索本人记忆（隔离良好 ✅）；但 WebUI 的 admin 角色可浏览/搜索任意用户记忆全文（[server.py:578-620](src/plugins/nonebot_plugin_webui/server.py#L578-L620)）——属设计意图，但需明确 admin 角色的可信边界。

### 4. 群聊滥用面

- `GROUP_REPLY_PROBABILITY=0`（仅 @/昵称触发）✅；但任何群成员可直接 `@Bot` 消耗 LLM 额度，且记忆命令 `删除记忆` 类仅 super 可用 ✅。无单用户/单群频率上限（除群冷却 5s），恶意刷量可耗尽 API 额度。

### 5. 安全基线（做得好的方面 ✅）

- 管理命令（`/admin`、`/auth`）全部 `SUPERUSER` 权限门禁；
- 数据库操作普遍使用参数化查询（`?` 占位），未发现直接拼接的 SQL 注入；
- 无 `shell=True` / `os.system` / `eval` 等危险调用（subprocess 均为列表参数形式）；
- Session 签名使用 HMAC-SHA256 + 每次启动随机密钥（重启即全员登出）；`SameSite=Strict`；
- `.env` 已正确加入 `.gitignore`，Git 历史中无密钥、无用户数据；
- .env 修改采用"备份 + 临时文件 + `os.replace` 原子写入"；
- 备份恢复仅按白名单文件名落地（除 Zip Slip 缺陷外）；
- FTS5/LIKE 检索均为参数化绑定。

---

## 五、风险汇总

| 级别 | 编号 | 风险 | 攻击面 | 可利用前提 |
|---|---|---|---|---|
| 高 | H1 | WebUI 存储型 XSS（未转义渲染） | WebUI（super 视图） | 审计条目注入（当前需 super 交互，未来任何非转义数据源即可触发） |
| 高 | H2 | 会话 Cookie 无 HttpOnly | WebUI | 任一 XSS |
| 高 | H3 | 图片下载路径穿越 → 任意文件写入 | QQ 消息 | 协议端允许自定义 `file` 字段 |
| 高 | H4 | 语音/图片 URL SSRF + 磁盘耗尽 | QQ 消息 | 消息段 URL 可控 |
| 高 | H5 | 备份恢复 Zip Slip → 任意文件写入 | WebUI（super） | super 会话（含被 H1/H2 劫持） |
| 中 | M1 | 记忆库路径穿越（Windows 分隔符） | WebUI（admin+） | admin 会话 |
| 中 | M2 | 密钥经 WebUI 读取/备份导出 | WebUI（admin+） | admin 会话 |
| 中 | M3 | 登录/API 无速率限制 | WebUI | 本机任意进程 |
| 中 | M4 | X-Forwarded-For 伪造 | WebUI | 任意连接 |
| 中 | M5 | 无登出/会话吊销 | WebUI | 会话泄露 |
| 中 | M6 | FTS5 查询语法注入 | QQ 消息 | 任意用户 |
| 中 | M7 | torch.load 不安全反序列化 | 供应链 | 模型文件被替换 |
| 中 | M8 | 依赖版本未锁定 | 供应链 | 上游发布恶意版本 |
| 低 | L1-L9 / E1-E10 | 见上文明细 | — | — |

**总体结论**：项目整体安全意识良好（权限门禁、参数化 SQL、密钥不入库、本地监听），未发现**无需任何前提即可远程无条件利用**的严重漏洞；主要风险集中在 **Web 管理后台的会话安全与输出转义**（H1/H2/H5 组合可在获得一次 XSS 或 super 会话后达成 RCE 级别后果），以及 **消息驱动下载的 SSRF/任意文件写入**（H3/H4）。作为长期运行、持有用户隐私数据并具备 LLM 调用能力的主机进程，建议按优先级落实整改。

---

## 六、整改建议（按优先级）

**P0 —— 立即处理（1 周内）**
1. 修复 H1/H2：所有 `innerHTML` 拼接点改为 `escapeHtml()`（或改 `textContent` 构建）；Cookie 改由服务端 `Set-Cookie` 下发并加 `HttpOnly`。
2. 修复 H3：图片/语音下载文件名强制 `os.path.basename` + 白名单后缀。
3. 修复 H5：备份解压逐成员路径校验（防 Zip Slip）。
4. 轮换 `DEEPSEEK_API_KEY`；清理 `data/env_backups/` 明文历史副本。

**P1 —— 短期（1 个月内）**
5. 修复 H4：下载限协议 https、限大小、限超时、拒绝内网地址段。
6. 修复 M1：`user_id` 白名单校验（仅数字）。
7. 修复 M2/M4：`config.view` 与备份下载收敛至 super；XFF 仅受信代理下使用。
8. 修复 M3：登录/轮询接口限流。
9. 生成依赖锁定文件，部署脚本改用锁定清单。

**P2 —— 中期（3 个月内）**
10. 修复 M5：登出与会话吊销。
11. 修复 M7：`torch.load(..., weights_only=True)`（或等价安全加载）；模型文件发布哈希校验。
12. 修复 M6/L1：FTS5 token 过滤保留字符；Jinja2 开启 `autoescape`。
13. 缓存目录（image_cache/voice_cache）增加容量上限与定期清理任务。
14. 运行账户降权（受限 Windows 账户）并收紧项目目录 ACL；为 WebUI 加 HTTPS（本地自签或反向代理）。

**P3 —— 长期**
15. 建立 LLM 提示词注入缓解策略（记忆边界指令、敏感信息过滤）。
16. 日志与隐私：消息内容脱敏、留存周期声明、备份加密。
17. 引入依赖漏洞扫描（pip-audit / osv-scanner）与 CI 安全检查（ruff 安全规则集）。

---

*报告完*
